# AIDictation v0.0.61

## Fixes
- Repackages the macOS release as a draggable installer DMG with `AIDictation.app` and an `Applications` shortcut.

## Technical
- Updates the release script to preserve the Finder installer layout before signing and notarizing the DMG.
- Bumps macOS app version to 0.0.61.
