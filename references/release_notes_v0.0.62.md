# AIDictation v0.0.62

## Fixes
- Repackages the macOS installer DMG with `create-dmg` so it opens as a standard drag-to-Applications installer.

## Technical
- Makes `create-dmg` the release script source for DMG layout instead of hand-rolled Finder scripting.
- Bumps macOS app version to 0.0.62.
