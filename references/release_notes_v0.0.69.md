Stops dictation from touching the clipboard.

- Inserts dictated text directly through macOS Accessibility and Unicode key events.
- Removes the temporary clipboard paste fallback from auto-insert and text formatting.
- Stops normal dictation and command mode from reading clipboard content implicitly.
